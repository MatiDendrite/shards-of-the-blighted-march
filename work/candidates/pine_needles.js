// Independent alternative: branch fans composed of individual tapered needles.
export default function generate(T){const g=new T.Group(),bark=new T.MeshStandardMaterial({color:0x49382d}),leaf=new T.MeshStandardMaterial({color:0x34463a,roughness:1});const trunk=new T.Mesh(new T.CylinderGeometry(.06,.22,6,8),bark);trunk.position.y=3;g.add(trunk);for(let tier=0;tier<7;tier++)for(let b=0;b<8;b++){const a=b*Math.PI/4+tier*.8,len=1.8-tier*.21;for(let k=0;k<6;k++){const t=(k+1)/6;const needle=new T.Mesh(new T.ConeGeometry(.16,len*.8,4),leaf);needle.position.set(Math.sin(a)*len*t,1.3+tier*.7-.2*t,Math.cos(a)*len*t);needle.rotation.z=Math.sin(a)*1.0;needle.rotation.x=Math.cos(a)*1.0;g.add(needle);}}return g;}
